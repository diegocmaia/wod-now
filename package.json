{
  "name": "wod-now",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "prisma:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:status": "prisma migrate status",
    "db:migrate:deploy:iam": "node scripts/prisma-with-rds-iam.mjs prisma migrate deploy",
    "db:migrate:status:iam": "node scripts/prisma-with-rds-iam.mjs prisma migrate status",
    "typecheck": "tsc --noEmit",
    "test": "npm run prisma:generate && vitest run && npm run typecheck",
    "seed": "node --import=tsx prisma/seed.ts",
    "db:seed": "npm run seed",
    "db:seed:iam": "node scripts/prisma-with-rds-iam.mjs node --import=tsx prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "playwright": "^1.58.2",
    "prisma": "^6.2.1",
    "tsx": "^4.21.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@aws-sdk/credential-providers": "^3.990.0",
    "@aws-sdk/rds-signer": "^3.990.0",
    "@prisma/client": "^6.2.1",
    "@types/react": "^19.2.13",
    "@types/react-dom": "^19.2.3",
    "@vercel/functions": "^3.4.2",
    "next": "^15.5.12",
    "pg": "^8.18.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "zod": "^4.3.6"
  }
}
